# "spring.cloud.config.uri" is no longer required because it will be bound by config-server service.
# you can also use this property for local development
# spring.cloud.config.uri=${vcap.services.config-server.credentials.uri:http://localhost:8888}
spring.cloud.config.label=spring-cloud-services
spring.application.name=membership